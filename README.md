# ttt-web

Static web pages for **Tee Time Trainer**, served via GitHub Pages.

- **`/cutline/`** — Cut Line beta landing page (`index.html`, built from the
  ship-now "Beta" static export). Go-live variant kept alongside as
  `Cut Line - Go Live.html`.

Plain HTML + CSS, no build step. The root redirects to `/cutline/`.

Generated by NOVA.
